Add experimental deformable visual meshes with procedural builder and USD
import support, shared `DeformableVisuals` points and normals across viewers and
`SensorTiledCamera` RGB/depth output, independent coarse-surface visibility,
and textured cable, cloth, and volume examples with procedural and USD setup
modes.
