Add optional `SolverCoupledADMM.Config.contact_max_triangle_pairs` and
`contact_reduction_hashtable_size_factor` controls for internal collision storage,
preserving default allocations and validating the matching-dependent capacity limit.
