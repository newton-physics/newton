Add per-DOF dry Coulomb joint friction to `SolverVBD` for revolute, prismatic,
and D6 joints with `rigid_compliant_alm=True`. Interpret
`Model.joint_friction` as an absolute force or torque bound [N or N·m],
providing static sticking at convergence and saturated sliding. Add the
`vbd_joint_friction` and `vbd_joint_friction_pendulum` examples. Positive
friction on mimic-linked joints is rejected; set friction to zero on both
reference and follower joints. D6 friction supports up to three orthonormal
linear axes and three orthonormal angular axes.
