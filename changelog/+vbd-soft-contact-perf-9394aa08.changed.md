Speed up `SolverVBD` rigid-soft contact and deformable elasticity on CUDA: gather contact forces per particle instead of scanning the whole contact buffer each iteration (best when active contacts are far fewer than capacity), cull far-apart candidates before the SDF search, pack two cloth particles per warp, and add a tetrahedra-only elasticity kernel. Contact capacity and the CPU and deterministic paths are unchanged.
