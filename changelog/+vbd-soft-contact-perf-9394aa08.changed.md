Accelerate `SolverVBD` rigid-soft contact processing and deformable elasticity on CUDA: gather contact forces per particle over the active contact prefix instead of scanning the full contact capacity every color and iteration, sort contact-detection candidate pairs by shape, reject far-apart edge/face candidates with a conservative AABB test before the SDF search, pack two cloth particles per warp in the tiled elasticity solve, and select a tetrahedra-only elasticity kernel when triangle and edge stiffnesses are inactive. Solver settings, contact capacity, and simulation results are unchanged; CPU and deterministic modes keep the previous kernels.
