Add opt-in registered USD schema fallback precedence to `ModelBuilder.add_usd()`, with one migration warning when future precedence changes an interpreted property or its meaning. Pass `use_registered_schema_fallbacks=True` to adopt the new precedence; see the USD parsing guide for ways to preserve current results.
