Add opt-in registered USD schema fallback precedence to `ModelBuilder.add_usd()`. Pass `use_registered_schema_fallbacks=True` to adopt the new precedence or `audit_registered_schema_fallbacks=True` to retain legacy results while reporting interpreted migration changes; see the USD parsing guide for ways to preserve current results.
