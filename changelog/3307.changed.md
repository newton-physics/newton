Add opt-in registered USD schema fallback precedence to `ModelBuilder.add_usd()` with migration warnings for affected prims. Pass `use_applied_schema_fallbacks=True` to adopt the new precedence, explicitly author values to retain current results, or explicitly pass `enable_self_collisions`, `mesh_maxhullvert`, or `joint_drive_gains_scaling` to override their resolved USD values in the opt-in mode.
