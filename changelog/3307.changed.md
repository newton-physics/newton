Add opt-in registered USD schema fallback precedence to `ModelBuilder.add_usd()` with migration warnings for affected prims when the policy is omitted. Pass `use_applied_schema_fallbacks=True` to adopt the new precedence, pass `False` explicitly to pin legacy behavior without migration warnings, explicitly author values to retain current results, or explicitly pass `enable_self_collisions`, `mesh_maxhullvert`, `joint_drive_gains_scaling`, or `collapse_fixed_joints` to override their resolved USD values in the opt-in mode.
