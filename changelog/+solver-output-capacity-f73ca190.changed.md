Allocate contact-indexed solver outputs eagerly from the model's collision-pipeline capacities. Construct `CollisionPipeline` before `solver.outputs(flags)` instead of passing `contacts=` to allocation, and pass matching `Contacts` to `solver.step()`. Use `None` for uninitialized model contact capacities and zero for a valid empty capacity; capacities are frozen once contact-indexed outputs are allocated.
