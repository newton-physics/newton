Change experimental `ModelBuilder.add_joint_rod()` to use the initial anchor separation as structural rest by default, so separated anchors start unstrained. Explicitly author a zero-translation rest pose to retain the previous pre-strained behavior.
