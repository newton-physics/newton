Fix `ModelBuilder.add_urdf(..., scale=)` leaving mimic-joint coefficients in source units, so a scaled prismatic follower no longer drifts from the authored `offset + multiplier * q_reference` relation.
