Add experimental `DriveNeuralGRU` support for ONNX checkpoints evaluated with Warp-NN, using metadata-selected joint, target and error features plus at most one caller-supplied column named in the checkpoint's `custom_inputs` metadata; drives list the arrays the caller must supply in `DriveBase.custom_inputs`, and `Actuator.step()` reads them from its `sim_state` or `sim_control` argument.
