Author `0.005 m` torsional and rolling resistance for both XPBD and VBD on the shared `basic_conveyor` belt and bags, and expose its existing substep budget through `--substeps`. The historical 10-substep default is unchanged; `--substeps 4` selects the measured accuracy/performance knee used for issue 3377.
