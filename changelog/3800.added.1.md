Add prefix-first cable geometry/frame helpers: `newton.utils.cable_straight_points()`, `cable_parallel_transport_quaternions()`, and `cable_straight_points_and_quaternions()`. Add `RodStiffness` and `rod_stiffness_from_elastic_moduli()` for per-joint rod stiffness.
