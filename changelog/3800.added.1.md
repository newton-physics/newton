Add `newton.utils.RodStiffness` and the `rod_compute_stiffness_from_elastic_moduli()`, `cable_generate_straight_points()`, `rod_compute_parallel_transport_quaternions()`, and `rod_generate_straight_points_and_quaternions()` helpers. The rod stiffness factory requires exactly one of `poissons_ratio` and `shear_modulus` and always returns a three-field `RodStiffness` containing stretch, bend, and twist.
