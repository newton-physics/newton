Add `newton.Rod` as the public input type for discrete-rod geometry, topology, frames, and optional elastic material or a complete set of section rigidities. `ModelBuilder.add_rod()` accepts exactly one of ordered point lists through `positions=` or a prepared object through `rod=`, including explicit graph topology; constitutive inputs are discretized per generated joint for supported non-branching assembly topology, while direct stiffness and damping remain builder arguments.
