Add the `newton.rod` public module with `RodStiffness`, `stiffness_from_elastic_moduli()`, `generate_straight_points()`, `compute_parallel_transport_quaternions()`, and `generate_straight_points_and_quaternions()`. The rod stiffness factory requires exactly one of `poissons_ratio` and `shear_modulus` and always returns a three-field `RodStiffness` containing stretch, bend, and twist.
