Fix `NameError: name 'dtype' is not defined` when KAMINO builds the `jacobi_diagonal` sparse preconditioner, so `block_sparse_ATA_inv_diagonal_2d` and the sparse forward-kinematics solves that use it run again.
