Fix `SolverMuJoCo` pinning ball loop-closing joints at the wrong point when the model is not assembled with the loop closed at the reference configuration. The synthesized `CONNECT` equality now takes its body2-side anchor from the joint's authored child frame (`joint_X_c`) instead of projecting the parent anchor through the reference body poses.
