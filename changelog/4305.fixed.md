Convert USD joint-state angular velocities (`state:angular:physics:velocity` and rotational D6 `state:rot*:physics:velocity`) from degrees per second to radians per second on import, matching the existing conversion of initial joint positions.
