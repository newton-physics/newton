Preserve explicitly disabled MJCF hinge and slide limits without enforcing their ranges in generic solvers, and retain the ranges during MuJoCo export and joint-property updates.
Match native MuJoCo's automatic-limit handling, including rejecting nonzero ranges without an explicit `limited` setting when `compiler autolimits="false"`.
