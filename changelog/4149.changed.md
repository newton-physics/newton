Size `SolverVBD` per-body soft-contact lists exactly, so rigid bodies react to every contact; contacts beyond the per-body buffer were previously dropped silently. Deprecate `rigid_body_particle_contact_buffer_size`: the capacity knob is no longer needed and the argument is ignored.
