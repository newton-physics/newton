VBD rigid-rigid contacts now consume `ShapeConfig.mu_torsional` and `ShapeConfig.mu_rolling`. Because both shape defaults are nonzero, existing VBD scenes gain angular friction unless they explicitly set the corresponding coefficients to zero. Set both coefficients to zero to recover the previous sliding-only VBD behavior.
