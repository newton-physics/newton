With `rigid_compliant_alm=True`, `SolverVBD` now enables torsional and rolling friction through the existing nonzero `ShapeConfig.mu_torsional` and `ShapeConfig.mu_rolling` defaults. Set both coefficients to zero on either shape in a contact pair to recover the previous sliding-only behavior for that pair. Legacy contacts remain sliding-only.
