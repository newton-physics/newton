`SolverVBD` now enables torsional and rolling friction by default for rigid-rigid contacts because `ShapeConfig.mu_torsional` and `ShapeConfig.mu_rolling` have nonzero defaults. Set both coefficients to zero on either shape in a contact pair to recover the previous sliding-only behavior for that pair.
