Use `ShapeConfig.mu_torsional` and `ShapeConfig.mu_rolling` for VBD rigid-rigid contacts. Both coefficients default to nonzero values. Set both to zero on either shape in a contact pair to recover the previous sliding-only behavior for that pair.
