Apply the `ModelBuilder.add_mjcf()` `scale` argument to MJCF equality weld `relpose` translations, which were imported at their authored length while the weld `anchor` beside them was scaled. A scaled import therefore welded bodies at the wrong relative offset. Both the body-based and site-based weld forms are covered, and the `relpose` rotation is left alone.
