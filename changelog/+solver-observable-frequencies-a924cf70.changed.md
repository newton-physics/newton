Require row-frequency declarations on experimental solver observable containers, with flag values naming array fields and `SUPPORTED_OBSERVABLE_FLAGS` as the sole capability set. Add `AttributeFrequency.CONTACT`, `CONTACT_RIGID`, and `CONTACT_SOFT` for capacity-based contact indexing. Derive contact allocation dependencies from frequencies, and allow `ArticulationView` to select solver observables with supported static row layouts without registering fields on the model. Dynamic contact selection and soft-contact force sensing remain unsupported.
