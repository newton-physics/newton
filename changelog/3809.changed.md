Align the experimental USD deformable importer with AOUSD proposal revision `63c74d79`. Read surface and curve `physics:thicknesses` from simulation geometry with the required `physics:thicknesses:elementType`, use the 1 mm physical fallback, and apply varying thickness to mass, collision radius, and derived stiffness. Read typed `physics:masses` as constant, per-simplex, per-curve, or per-point values and convert them through element masses before lowering to Newton particles or cable segments. Missing cloth bend arrays now follow `physics:restBendAnglesDefault` (`flat` by default), and rest fallbacks read simulation attributes at the USD default time. Unsupported authored rest topology, an element-type token without its array, invalid body mass/density overrides, and surface/cable stiffness outside Newton's finite float32 range now warn and use safe fallbacks. Remove unsupported rest topology or bake it into default-time simulation topology, author array/type pairs together, and replace invalid physical values; valid assets within Newton's supported subset require no migration.
