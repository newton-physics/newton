Add a canonical limit-bilateral-contact (`L -> B -> C`) coupling schedule and an opt-in deterministic dense/sparse associated Coulomb-cone APGD contact backend to `SolverKamino` DVI. Also add time-based stabilization and recovery-speed controls for bilateral joints, joint limits, and contacts; independent physical compliance for kinematic bilateral joints, active joint limits, and contacts while leaving dynamic actuator and bounded friction/effort rows unchanged; graph-conditional APGD iteration; solver diagnostics; validated contact-triplet preconditioning; and an optional, default-off post-stabilization bilateral refresh.
