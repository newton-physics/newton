Add a canonical limit-bilateral-contact (`L -> B -> C`) coupling schedule, controlled by `coupling_iterations` with a default of two passes, independently configurable `limit_pgs_sweeps` and `contact_pgs_sweeps` local budgets, and an opt-in deterministic dense/sparse associated Coulomb-cone APGD contact backend to `SolverKamino` DVI. The explicit controls replace the interim `max_alternating_iterations` and `inequality_sweeps_per_iteration` fields; `bilateral_solve_interval` is removed because `B` is now visited once per coupling pass. Also add time-based stabilization and recovery-speed controls for bilateral joints, joint limits, and contacts; independent physical compliance for kinematic bilateral joints, active joint limits, and contacts while leaving dynamic actuator and bounded friction/effort rows unchanged; graph-conditional APGD iteration; solver diagnostics; validated contact-triplet preconditioning; and an optional, default-off post-stabilization bilateral refresh.
