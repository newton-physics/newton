Sort soft-contact candidate pairs by shape on every device. Same contacts, different order, so CPU and deterministic results can shift within floating-point tolerance. Faster on scenes with many shapes or worlds; a little slower on small dense-contact scenes.
