Add independent initial and structural-rest poses for experimental Rod joints and rod-built cables through the `rest_rotation`, `rest_positions`, `rest_node_positions`, and `rest_quaternions` builder inputs. Rod structural-rest anchors coincide, so live anchor separation is stretch/shear strain. Use `rest_straight=True` with `ModelBuilder.add_rod()` to keep initial poses, joint anchors, and segment lengths unchanged while authoring zero intrinsic bend and twist.
