Add independent initial and structural-rest poses for experimental Rod joints and rod-built cables through the `rest_xform`, `rest_positions`, `rest_node_positions`, and `rest_quaternions` builder inputs. Use `rest_straight=True` with `ModelBuilder.add_rod()` to keep initial poses, joint anchors, segment lengths, and any closing rest translation unchanged while authoring zero intrinsic bend and twist.
