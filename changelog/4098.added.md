Add `Actuator.State.assign()` for copying actuator state between the caller's two state objects. A CUDA graph does not record the host-side state swap, so a captured region holding an odd number of actuator steps must assign at its boundary for `Delay`, `DrivePID` and `DriveNeuralLSTM` state to advance on replay.
