Add torsional and rolling friction to `SolverVBD` rigid-rigid contacts through `ShapeConfig.mu_torsional` and `ShapeConfig.mu_rolling`, including angular-friction torque transfer through VBD proxy coupling.
