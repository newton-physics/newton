Add torsional and rolling friction to `SolverVBD` rigid-rigid contacts with `rigid_compliant_alm=True` through `ShapeConfig.mu_torsional` and `ShapeConfig.mu_rolling`, including angular-friction torque transfer through VBD proxy coupling.
