Add `ModelBuilder.add_cable_spline()` for building cables along Catmull-Rom splines through control points, with rotation-minimizing per-segment frames, uniform arc-length sampling, optional twist, and closed loops. Add `newton.utils.cable_spline_shape()` returning a `CableSplineShape` with both the posed spline geometry and, via `straight_rest_shape=True`, the cable's natural rest configuration (a straight chain, or a circle for closed cables) so the spline only poses the initial state written into `State.body_q` (the cable-routing workflow). `CableSplineShape.body_transforms()` yields the per-body transforms for that step.
