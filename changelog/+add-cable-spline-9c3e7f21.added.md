Add `ModelBuilder.add_cable_spline()` for building cables along Catmull-Rom splines through control points, with rotation-minimizing per-segment frames, uniform arc-length sampling, optional twist, and closed loops; add `newton.utils.create_cable_spline_points()`, `newton.utils.create_rotation_minimizing_cable_quaternions()`, `newton.utils.create_cable_spline_shape()`, and `newton.utils.create_cable_body_transforms()`. A `straight_rest_shape` option builds the cable at its natural (straight or, for closed cables, circular) rest configuration so the spline only poses the initial state — the cable-routing workflow.
