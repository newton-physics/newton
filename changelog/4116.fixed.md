Assign the model-derived deterministic record bound to every MuJoCo Warp module
whose kernels run atomics inside data-dependent loops, so `SolverMuJoCo` no
longer overflows the deterministic scatter buffer in `_update_gradient_JTCJ_dense`
and other dynamic-atomic kernels under `wp.DeterministicMode.RUN_TO_RUN`.
