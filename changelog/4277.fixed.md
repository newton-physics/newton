Restore the stress-transpose normalization in `SolverImplicitMPM` to preserve virtual work and the configured elastic stiffness. Bound the dropped spherical/deviatoric coupling in nonlinear iterations while retaining the existing plastic-strain reconstruction factors.

For scenes tuned against the previous normalization, multiply `mpm.young_modulus`, `mpm.yield_pressure`, `mpm.yield_stress`, and `mpm.viscosity` by 0.5 for the corresponding legacy stress-scale conversion. This assumes matched initial history and unchanged rigidity/cutoff classification; it does not guarantee identical finite-iteration trajectories or convert saved stress/warmstart state. Keep physically specified material constants unchanged when adopting the corrected normalization.
