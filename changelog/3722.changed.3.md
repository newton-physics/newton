Derive each cable joint's stiffness in the experimental USD deformable importer from its local dual rest length, `0.5 * (L_parent + L_child)`, instead of a curve-wide mean, and use a welded graph's authored `restShapePoints` for that normalization. Re-test unevenly sampled or welded cables with authored rest shapes; their imported stiffness may differ from 1.5.0. A non-finite or zero-length rest segment now emits a warning instead of being ignored silently.
