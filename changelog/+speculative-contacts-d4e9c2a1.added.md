Add opt-in speculative rigid-contact manifolds through `CollisionPipeline.SpeculativeContactConfig`, the `speculative_config` constructor argument, and per-call `CollisionPipeline.collide(..., dt=...)` horizon overrides, with continuous swept-AABB filtering and predictive contact-reduction lanes, and reject unsupported hydroelastic combinations.
