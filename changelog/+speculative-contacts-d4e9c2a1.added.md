Add opt-in speculative rigid-contact manifolds through `CollisionPipeline.SpeculativeContactConfig`, the `speculative_config` constructor argument, and `CollisionPipeline.collide(..., dt=...)`, with continuous swept-AABB filtering, predictive contact-reduction lanes, and speculative narrow-phase and SDF kernel options. Configure prediction with `CollisionPipeline.SpeculativeContactConfig` and override its collision horizon per call with `collide(..., dt=...)`.
